on: [push]

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: afc163/surge-preview@main
        with:
          surge_token: ${{ secrets.SURGE_TOKEN }}
          build: |
            npm install
            npm run build
          dist: public
